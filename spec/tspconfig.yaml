# Конфигурация компиляции TypeSpec → OpenAPI 3.
# Запуск: npm run spec:build (из корня проекта).
emit:
  - "@typespec/openapi3"
options:
  "@typespec/openapi3":
    emitter-output-dir: "{project-root}/../docs/api"
    output-file: "openapi.yaml"
